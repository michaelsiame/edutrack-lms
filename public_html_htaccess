<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect all traffic to public/ directory
    RewriteCond %{HTTP_HOST} ^(www\.)?edutrackzambia\.com$ [NC]
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
